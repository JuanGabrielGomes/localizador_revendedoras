/**
 * Fonte de verdade movida para packages/shared/types.ts, compartilhada com o
 * app mobile (Expo) em mobile/. Reexportado aqui para não quebrar nenhum
 * import existente no app web.
 */
export * from "../packages/shared/types";
